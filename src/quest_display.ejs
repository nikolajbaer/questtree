<% function walk(q){ %>
    <div class="subquest">
     <strong>Task: <% if(q.satisfied){ %>✅<% } %><%= q.name %></strong>
     <% 
     if(q.subreq.length){ %><i>but first.. </i><% }
    q.subreq.forEach( subq => {
        walk(subq)
    })
} 

walk(quest);
%>

